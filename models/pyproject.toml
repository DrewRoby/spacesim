[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "spacesim-models"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    # stdlib only for now — add packages here as each layer is actually implemented:
    # numpy     ← heritability matrix math
    # scipy     ← statistical distributions in lifecycle model
    # pymc      ← Bayesian population modeling
    # scikit-learn / hdbscan ← archetype clustering
    # pydantic  ← strict state object validation
]
